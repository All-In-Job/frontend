export type ActivityHistory = {
  id: string;
  title: string;
  period: string;
};

export type ActivityListData = {
  path: string;
  id: string;
  category: string;
  keyword: string;
  activeTitle: string;
  activeContent: string;
  period: string;
  score: string;
};
