import styled from '@emotion/styled';

import HashTag from 'components/HashTag';

export const CustomHashTag = styled(HashTag)`
  margin-right: 8px;
  margin-top: 16px;
  white-space: nowrap;
`;

export const Container = styled.div`
  display: flex;
  flex-wrap: wrap;
`;
