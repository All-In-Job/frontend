import styled from '@emotion/styled';

const MenuFilter = () => {
  return <Wrapper>MenuFilter</Wrapper>;
};

export default MenuFilter;

const Wrapper = styled.div`
  padding: 32px;
  border-radius: 12px;
  background-color: ${props => props.theme.palette.background.secondary};
`;
